[tool.poetry]
name = "stamp"
version = "0.1.0"
description = "Stamp - The tool to record your working hours"
authors = ["Hannes Ortmeier <ortmeier.hannes@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
sqlite-utils = "^3.35.2"
tabulate = "^0.9.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
stamp = 'stamp:main'